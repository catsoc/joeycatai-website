---
import { SITE } from '@/site.config';

export interface Props {
  title: string;
  description?: string;
  image?: string;
  imageWidth?: number;
  imageHeight?: number;
  imageType?: string;
  article?: boolean;
  pubDate?: Date;
  updatedDate?: Date;
  tags?: string[];
  author?: string;
  robots?: string;
}

const {
  title,
  description = SITE.description,
  image,
  imageWidth,
  imageHeight,
  imageType,
  article = false,
  pubDate,
  updatedDate,
  tags = [],
  author = SITE.author,
  robots = 'index,follow',
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, SITE.url);
const rawImage = image || '/og-default.svg';
const ogImage = rawImage.startsWith('http') ? rawImage : new URL(rawImage, SITE.url);
const ogImageWidth = image ? imageWidth : 1200;
const ogImageHeight = image ? imageHeight : 630;
const ogImageType = image ? imageType : 'image/svg+xml';
const fullTitle = title.includes(SITE.name) ? title : `${title} · ${SITE.name}`;
---

<!-- Primary -->
<title>{fullTitle}</title>
<meta name="description" content={description} />
<meta name="author" content={author} />
<meta name="robots" content={robots} />
<link rel="canonical" href={canonicalURL} />

<!-- Open Graph（Facebook、Instagram、Threads 共用） -->
<meta property="og:type" content={article ? 'article' : 'website'} />
<meta property="og:url" content={canonicalURL} />
<meta property="og:title" content={fullTitle} />
<meta property="og:description" content={description} />
<meta property="og:image" content={String(ogImage)} />
<meta property="og:image:alt" content={fullTitle} />
{ogImageWidth && <meta property="og:image:width" content={String(ogImageWidth)} />}
{ogImageHeight && <meta property="og:image:height" content={String(ogImageHeight)} />}
{ogImageType && <meta property="og:image:type" content={ogImageType} />}
<meta property="og:site_name" content={SITE.name} />
<meta property="og:locale" content="zh_TW" />

<!-- Article 專屬 Open Graph -->
{article && (
  <>
    {pubDate && <meta property="article:published_time" content={pubDate.toISOString()} />}
    {updatedDate && <meta property="article:modified_time" content={updatedDate.toISOString()} />}
    <meta property="article:author" content={author} />
    {tags.map((tag) => <meta property="article:tag" content={tag} />)}
  </>
)}

<!-- Twitter / X -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:site" content={SITE.twitterHandle} />
<meta name="twitter:creator" content={SITE.twitterHandle} />
<meta name="twitter:title" content={fullTitle} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={String(ogImage)} />

<!-- Feed -->
<link rel="alternate" type="application/rss+xml" title={`${SITE.name} Blog`} href="/rss.xml" />
